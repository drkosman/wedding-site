Drop hero carousel photos in this folder.

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`, `.avif`.

The carousel loads files from this folder automatically and sorts them by filename.
Use names like `01-arrival.jpg`, `02-bay.webp`, and `03-party.jpg` to control the order.
